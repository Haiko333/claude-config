# === CREDENTIALS & SECRETS (NEVER push these) ===
.credentials.json
*.key
*.pem
*.p12
.env
.env.*
# API keys live in .env — ai-router-config.json and env.example (no secrets) ARE tracked
nim-config.json

# === CONVERSATION & SESSION DATA ===
history.jsonl
projects/
session-env/
sessions/
file-history/
tasks/
shell-snapshots/
paste-cache/
settings.local.json

# === CACHE & TEMPORARY ===
backups/
cache/
downloads/
ide/
stats-cache.json
mcp-needs-auth-cache.json

# === PLUGINS (runtime state & cached marketplace data) ===
plugins/cache/
plugins/data/
plugins/install-counts-cache.json
plugins/marketplaces/

# === AUDIO BINARIES (stored separately if needed) ===
song/

# === NODE / BUN ===
node_modules/
*.tsbuildinfo
dist/
.cache

# === LOCAL DOCS (machine-specific, not for sharing) ===
CLAUDE.md
scripts/CLAUDE.md

# === OS ===
.DS_Store
Thumbs.db
